public class StringRec
{
	public static String decompress(String compressedText)
	{
		if(compressedText.length() == 1 || compressedText.length() == 0)
		{
			return compressedText;
		}

		if(compressedText.substring(0,1))
	}
}